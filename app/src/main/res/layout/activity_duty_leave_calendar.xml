<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/dialog_background_gradient"
    android:id="@+id/calender_main_RL">

    <include
        android:id="@+id/header_mark_attendance"
        layout="@layout/mark_attendance_header" />

    <include
        layout="@layout/activity_duty_leave_calendar_new"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/header_mark_attendance"/>


</RelativeLayout>